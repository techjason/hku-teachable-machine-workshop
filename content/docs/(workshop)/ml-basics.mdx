---
title: Machine Learning Basics
description: Understanding the fundamentals of Machine Learning
---

## What is Machine Learning?

Machine Learning (ML) is a branch of artificial intelligence that enables computers to learn from examples rather than being explicitly programmed. Think of it like teaching a child:

- **Traditional Programming**:

  ```
  IF (sees_round_orange_object) AND (has_bumpy_texture) THEN "it's an orange"
  ```

- **Machine Learning**:
  Show many examples of oranges and non-oranges, and let the computer learn the patterns

<figure>
  ![Image](/deep-learning.png)
  <figcaption className="text-sm text-gray-500 text-center mt-2">
    Deep Learning is a subset of Machine Learning that processes data through
    multiple neural layers, similar to how human brains learn
    <span className="text-xs italic">
      {" "}
      Image source: [Wikimedia Commons](https://commons.wikimedia.org/wiki/File:MultiLayerNeuralNetworkBigger_english.png)
    </span>
  </figcaption>
</figure>

## Key Concepts

### 1. Pattern Recognition

Computers learn to recognize patterns in data. These patterns can be in:

- 🖼️ Images (what we'll work with)
- 🔊 Sound
- 📝 Text
- 🔢 Numbers
- 📊 Any type of data

### 2. Learning from Examples

Instead of writing rules, we:

1. Collect examples (data) → 2. Label them (training data) → 3. Let the computer find patterns → 4. Use these patterns to make predictions

<figure>
  ![Image](/ml-pipeline.png)
  <figcaption className="text-sm text-gray-500 text-center">
    A typical supervised learning pipeline: collecting data, training the model,
    and making predictions
  </figcaption>
</figure>

### 3. Types of Machine Learning

| Type                   | Description                                                                                                                      | Examples                                                           |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| Supervised Learning    | - What we'll use in Teachable Machine<br/>- We provide labeled examples<br/>- Computer learns to predict labels for new examples | - Image classification<br/>- Spam detection<br/>- Face recognition |
| Unsupervised Learning  | - Computer finds patterns without labels<br/>- Discovers natural groupings in data                                               | - Customer segmentation<br/>- Anomaly detection                    |
| Reinforcement Learning | - Learning through trial and error<br/>- Like training a pet with rewards                                                        | - Game playing AI<br/>- Robot navigation                           |

<SAQQuiz
  questions={[
    {
      question:
        "1a) Define what Machine Learning is and give two examples of its applications.",
      markScheme: [
        "Machine Learning enables computers to learn from examples rather than explicit programming [1]",
        "It involves recognizing patterns in data to make predictions [1]",
        "Examples: image classification, spam detection, face recognition (any two) [1]",
      ],
      marks: 3,
    },
    {
      question:
        "1b) Explain why Machine Learning is more suitable for complex pattern recognition compared to traditional programming.",
      markScheme: [
        "ML can handle unstructured and complex data patterns [1]",
        "It can adapt to new data and variations automatically [1]",
        "It doesn't require explicit rules for every possible scenario [1]",
        "It improves with more examples and experience [1]",
      ],
      marks: 2,
    },
    {
      question:
        "2) A developer wants to create a system that can identify different types of fruits in images. Compare how this would be approached using traditional programming versus machine learning.",
      markScheme: [
        "Traditional programming would require explicit rules for each fruit's characteristics [1]",
        "ML approach would learn patterns from many labeled examples of fruits [1]",
        "ML can handle variations in lighting, angle, and size automatically [1]",
        "ML system can generalize to new examples it hasn't seen before [1]",
        "ML approach is more scalable for adding new fruit types [1]",
      ],
      marks: 3,
    },
  ]}
/>
## How Computers "See" Images

When computers process images, they don't "see" like we do. Instead, they work with a grid of numbers representing pixel values. Each pixel contains information about color intensity, typically in RGB (Red, Green, Blue) format. For example, a bright red pixel might be represented as [255, 0, 0], while a dark blue would be [0, 0, 100]. Machine learning models analyze these numerical patterns to identify objects and features within images.

1. **Pixels**

   - Images are grids of numbers
   - Each pixel has color values (RGB)
   - Example:
     ```
     Red pixel: [255, 0, 0]
     Green pixel: [0, 255, 0]
     Blue pixel: [0, 0, 255]
     ```

2. **Features**

Features are the building blocks that machine learning models use to understand images. At the most basic level, they start with patterns of pixels - recurring arrangements of color values that might indicate something meaningful. The model then identifies key visual elements like edges where colors change sharply, corners where edges meet, and textures that show repeating patterns.

It also analyzes colors and shapes, looking for distinctive hues and geometric forms that could help identify objects. These simpler features are then combined into more complex patterns - for example, several edges might form the outline of an object, while combinations of colors and textures could indicate specific materials or surfaces.

### From Pixels to Predictions

1. Image input → Grid of numbers
2. Find important features
3. Learn which features match which classes
4. Make predictions based on features

## Practical Exercise

Before moving on, try this thought exercise:

1. Think of how you recognize a cat:

   - What features do you look for?
   - How many examples did you need to learn?
   - How do you handle variations?

2. Now imagine teaching a computer:
   - What features might it need?
   - How many examples would help?
   - What variations should we include?

## Next Steps

Ready to start working with classes and features? Continue to the [Classes and Features](/docs/classes-features) section!
